# calculator-cpp
It's a calculator made in C++. What do you expect?

##### Current features:
- tanh-sinh quadrature
- Monte-Carlo integration
- `Symbol` class that doesn't do anything
##### Plans:
- Make an `Expression` class, combining multiple `Symbol`s and `Literal`s, which hopefully becomes able to be constructed into a function.